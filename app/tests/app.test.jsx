var expect = require('expect');



describe('App', () => {
  it('Just a little test', () => {
    expect(1).toBe(1);
  });

  it('This is another little test', () => {
    expect(1).toBe(1);
  });
});
